1. It waits for all promises get resolved
2. It returns an array of all resolved values in the `same order`
3. Rejects immediately if any promise get rejected
4. Use when : all operation to succeed and want to process result together
