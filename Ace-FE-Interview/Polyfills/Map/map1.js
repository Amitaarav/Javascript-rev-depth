// 1st

// map returns new array by transforming the elements of original array
// for is declarative and map is functional
Array.map(callbackFn, thisArg){
    function callbackFn(element,index,array){
        
    }
}
// thisArg is optional
