A closure is like a function along with its lexical environment/scope

let has block-scope. Each and every time when setTimeout method is called the variable i get initialize with new value.

i = 2 // is a new copy of variable
// different memory location
